# rc.13 模块整理验证记录

日期：2026-09-12。基线：`c55caf0bc94cec3053b3779e0474ed1aadff048f`（rc.12）。测试在 G 盘隔离 worktree 执行，没有打开生产数据库、开始录音或调用付费云服务。

| 检查 | 本轮结果 |
| --- | --- |
| 完整主进程清单，4 个并行 worker | **2,388 passed / 5 skipped / 0 failed**；共 2,393 项，约 142 秒 |
| 渲染器（43 个测试文件） | **438 passed / 0 failed** |
| 新增模块契约 / 总结交互 | 7 项主进程＋2 项 UI 测试，包含在上述计数中 |
| TypeScript | 通过 |
| ESLint | 0 errors；3 条既有 React Fast Refresh warnings |
| Renderer production build | 通过；存在大于 500 kB 的既有 chunk 提示 |
| 版本、lockfile、changelog、tag 规则 | `jarvis-v0.2.0-rc.13` 通过 |
| 提取前后语法树对比 | 69 个函数/类一致（排除格式/注释差异） |
| 文档路径核验 | 19 个本地链接、40 个功能表源码引用存在；其余功能说明人工核对调用模块 |
| 暂存区 | diff whitespace 检查通过；无密钥模式命中，无大于 1 MB 的文件，无运行数据/权重 |

五项跳过：两项 Windows 符号链接权限测试、未准备的 ffmpeg-static 实体 round-trip、未授权配置的私人声纹评估、未随源码下载的真实 Silero 模型。跳过不是通过，也不代表运行版本缺失这些组件。

测试过程中修正了新工作区依赖准备、一个写死旧 G 盘临时目录的 fixture，以及依赖本机端口可绑定性的模拟生命周期测试；端口发现现可注入，生产默认逻辑不变。抽取过程中发现并恢复了工厂仍需要的 `createHash` 导入，完整测试在修正后重跑。版本断言也同步到 rc.13。

## 保留的验收边界

没有构建/安装新 Windows 分发包，没有重新处理历史录音，没有实体麦克风/WASAPI 的人工验收、真实 CUDA 推理、真人声纹评分或三小时耐久。旧版的验收不自动继承为 rc.13 二进制的验收。该候选只能作为已验证源码结构检查点，不能标为最终稳定版。

历史方案文档保留其原有时间背景；本轮新增功能清单按实际源码组织，没有把历史计划逐项完成视为事实。数据库 schema 仍为 v60，两个大仓储的事务 SQL 和历史迁移尚未全部拆分。
